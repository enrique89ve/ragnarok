# Interface harness log

- Card Layout Canvas: the `keywords` slot is a child rail inside the `description` slot, not an independent full-width layer.
- Default keyword geometry is centered (`x: 27`, `w: 46`) and the real frame mirrors that width/alignment so the editor overlay and rendered card agree.
- Draft storage moved from `v3` to `v4` so a previously saved overlapping baseline cannot mask the corrected default.
- Verified visually at 1440x900 and checked for horizontal overflow at 390, 768, and 1440 widths.
- The editor's Tribe band is a flex slot; centering its box was insufficient, so the editor override now centers its text/content explicitly.
- Rarity now uses a lower, shorter bottom slot (`y: 96`, `h: 3.6`) and the stage uses a larger responsive zoom while preserving the card aspect ratio.
- Rarity was enlarged by 50% to `w: 33`, `h: 5.4`, with `x: 33.5` keeping its center aligned to the lower band; the intended default vertical position is `y: 96.7`.
- The shared card frame computed `overflow: hidden`, clipping the enlarged marker at `y: 96.7`; the editor-only frame now permits visible overflow so the marker can extend below the card edge without changing other card surfaces.
- The editor underlay was a second clipping ancestor; it now also allows visible overflow, while the art layer remains independently contained.
- Verified visually at 1440x900 and 390x844: the rendered rarity center is within 0.01px of the stage center, with no horizontal overflow or browser console errors.
