import {
	MAX_ARMOR as SHARED_MAX_ARMOR,
	MAX_BATTLEFIELD,
	MAX_HAND as SHARED_MAX_HAND,
	MAX_MANA,
} from '@shared/protocol-core/gameLimits';

export const MAX_BATTLEFIELD_SIZE = MAX_BATTLEFIELD;
export const MAX_ARMOR = SHARED_MAX_ARMOR;
export const MAX_HAND_SIZE = SHARED_MAX_HAND;
export { MAX_MANA };
